<% flash.each do |key, value| %>
  <% if value.is_a?(String) %>
    <div class="alert <%= key == 'notice' ? 'bg-success' : 'bg-danger' %>" role="alert">
      <em class="fa fa-check-circle mr-2"></em>
      <%= raw(value) %>
    </div>
  <% end %>
<% end %>
